meta {
  name: /login
  type: http
  seq: 1
}

post {
  url: http://localhost:3001/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nonce":"{{nonce_token}}",
    "token": "{{google_token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
